server:
  port: ${PS_SERVER_PORT:8081}

spring:
  application:
    name: prospecting-service
  datasource:
    url: jdbc:h2:mem:cnpjdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
